<h1>注文内容の確認・編集</h1>

<div>
	<h2>お届け先住所</h2>
	<p>送付先住所</p>
	<% form_for(@order) do |f| %>
		<label>
			<%= f.radio_button :shipping_address, @client.id, {:checked => true} %>
			<%= @client.last_name + @client.first_name, @client.post_code, @client.address %>
		</label>
		<% @client.address.each do |t| %>
			<label>
				<%= f.radio_button :shipping_address, t.id %>
				<%= t.last_name + t.first_name, t.sub_post_name, t.sub_address %>
			</label>
		<% end %>
</div>

		<%= f.submit "注文を確定する" %>
	<% end %>